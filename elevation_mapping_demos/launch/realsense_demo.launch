<launch>

  <!-- Launch the elevation mapping node -->
  <node pkg="elevation_mapping" type="elevation_mapping" name="elevation_mapping" output="screen">

    <!-- Config that loads topics, and various parameters for the mapping process -->
    <rosparam command="load" file="$(find elevation_mapping_demos)/config/elevation_maps/remove_object.yaml" />

    <!-- Camera parameters which are specific -->
    <rosparam command="load" file="$(find elevation_mapping)/config/sensor_processors/realsense_ZR300.yaml" />

    <!-- Postprocessing??? Unsure what this means -->
    <rosparam command="load" file="$(find elevation_mapping_demos)/config/postprocessing/postprocessor_pipeline.yaml" />
  </node>

  <!-- Launch visualizations for the resulting elevation map -->
  <include file="$(find elevation_mapping_demos)/launch/visualization.launch" />

  <!-- Launch RViz with the demo configuration -->
  <node name="rviz" pkg="rviz" type="rviz" args="-d $(find elevation_mapping_demos)/rviz/elevation_map_visualization_pointcloud.rviz" />

</launch>
